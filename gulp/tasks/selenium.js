//import {spawn} from 'child_process';

export default function(gulp, plugins, config, opts) {
  //const {data} = opts;

  //return (...args) => {
    //console.log('***ARGS*****', args);

    //return spawn('ls', ['-la', '.'], {stdio: 'inherit'});
  //};
}

